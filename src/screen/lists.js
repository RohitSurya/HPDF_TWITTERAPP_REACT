import React, { Component } from 'react';
import { StyleSheet, View, Text, Image} from 'react-native';

export default class Lists extends Component {
  render(){
    return(
      <View>
      <Text style={{fontSize:28}}>Lists page</Text>
      </View>
    );
  }
}
